<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.three.transfer.dao.TFileDao">

    <resultMap id="fileMap" type="TFile">
        <id property="fileId" column="file_id" />
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="create_time" property="createTime"/>
        <result column="last_edit_time" property="lastEditTime"/>
        <result column="file_download_time" property="fileDownloadTime"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_valid_time_mills" property="fileValidTimeMills"/>
        <association property="user" column="user_id" javaType="com.three.transfer.entity.User">
            <id column="user_id" property="userId"/>
        </association>
        <association property="fileCategory" column="file_category_id" javaType="com.three.transfer.entity.TFileCategory">
            <id column="file_category_id" property="fileCategoryId"/>
        </association>

    </resultMap>







    <insert id="insertFile" parameterType="com.three.transfer.entity.TFile"
            useGeneratedKeys="true" keyColumn="file_id" keyProperty="fileId">

        insert into tb_file(file_name, file_category_id, user_id, file_size, file_path, create_time, last_edit_time,
                            file_download_time, file_valid_time_mills)
        VALUES (
                  #{fileName}, #{fileCategory.fileCategoryId}, #{user.userId},#{fileSize}, #{filePath},
                  #{createTime}, #{lastEditTime}, #{fileDownloadTime}, #{fileValidTimeMills}
                );
    </insert>

    <select id="getFileById" parameterType="int" resultMap="fileMap">
        select f.file_id,
               f.file_name,
               f.file_category_id,
               f.user_id,
               f.file_size,
               f.file_path,
               f.create_time,
               f.last_edit_time,
               f.file_download_time,
               f.file_valid_time_mills,
               fc.file_category_id,
               u.user_id
        from tb_file as f, tb_file_category as fc, tb_user as u
        where
            f.user_id = u.user_id
          and
            f.file_category_id = fc.file_category_id
          and
            f.file_id = #{fileId};
    </select>

    <delete id="deleteFileById" parameterType="int">
        delete
        from tb_file
        where file_id = #{fileId}
    </delete>


    <select id="getFilesByUserId" parameterType="int" resultMap="fileMap">
        select f.file_id,
               f.file_name,
               f.file_category_id,
               f.user_id,
               f.file_size,
               f.file_path,
               f.create_time,
               f.last_edit_time,
               f.file_download_time,
               f.file_valid_time_mills,
               fc.file_category_id,
               u.user_id
        from tb_file as f, tb_file_category as fc, tb_user as u
        where
        f.user_id = u.user_id
        and
        f.file_category_id = fc.file_category_id
        and
        f.user_id = #{userId};
    </select>

    <select id="getFileByNameAndUserId" resultMap="fileMap">
        select f.file_id,
               f.file_name,
               f.file_category_id,
               f.user_id,
               f.file_size,
               f.file_path,
               f.create_time,
               f.last_edit_time,
               f.file_download_time,
               f.file_valid_time_mills,
               fc.file_category_id,
               u.user_id
        from tb_file as f, tb_file_category as fc, tb_user as u
        where
            f.user_id = u.user_id
          and
            f.file_category_id = fc.file_category_id
          and
            f.user_id = #{userId}
          and
            f.file_name = #{fileName};
    </select>

</mapper>